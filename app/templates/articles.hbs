
<h1>Artikkelit {{#link-to 'articles.new' class='ui-button'}}Uusi artikkeli{{/link-to}}</h1>

{{#memory-scroll key='sepot'}}
<ul class="article-list">

	{{#each sorted as |article index|}}

	<li>
		<article>
			<h1>{{#link-to 'articles.article' article.id}}{{article.title}}{{/link-to}}
				{{#if article.id}}
					<button {{action 'deleteArticle' article}}>x</button>
				{{/if}}
			</h1>
			<p class="date">{{moment-format article.published}}</p>
			<p>{{article.author.name}}</p>
			<p>{{article.lead}}</p>
		</article>
	</li>

	{{else}}
	<p>Ei artikkeleita :(</p>
	{{/each}}

</ul>
{{/memory-scroll}}

{{outlet}}
